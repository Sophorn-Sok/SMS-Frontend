import { FeaturePlaceholder } from "@/components/feature-placeholder";
import { roleFeatures } from "@/lib/features";

export default function ManageUserAccountsPage() {
  return <FeaturePlaceholder feature={roleFeatures["admin"][0]} />;
}
