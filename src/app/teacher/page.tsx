import { FeaturePlaceholder } from "@/components/feature-placeholder";
import { roleFeatures } from "@/lib/features";

export default function TeacherClassroomPage() {
  return <FeaturePlaceholder feature={roleFeatures["teacher"][0]} />;
}
